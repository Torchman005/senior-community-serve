<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC ".//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.luminous.senior_community_server.mapper.EmployeeMapper">


    <select id="selectAll" resultType="com.luminous.senior_community_server.bean.Employee">
        select * from employee
    </select>

    <insert id="insert" parameterType="com.luminous.senior_community_server.bean.Employee">
        insert into `employee` (username, password, role, name, sex, no, age, phone_number, department_name) values (#{username}, #{password}, #{role}, #{name}, #{sex}, #{no}, #{age}, #{phoneNumber}, #{departmentName})
    </insert>

    <update id="updateById" parameterType="com.luminous.senior_community_server.bean.Employee">
        update `employee` set username = #{username}, password = #{password}, role = #{role}, name = #{name}, sex = #{sex}, no = #{no}, age = #{age}, phone_number = #{phoneNumber}, department_name = #{departmentName} where id = #{id}
    </update>
</mapper>